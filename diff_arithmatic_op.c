#include<stdio.h>
#include<math.h>

int main()

{
    // to perform different arithmatic operations 

    int a=10,b=10,c;
    printf("the sum of a and b %d \n",a+b);
    printf("the substraction of a and b %d \n",a-b);
    printf("the multiplication of a and b %d \n",a*b);
    printf("the division of a and b is %f \n",a/b);
    

    printf("the power of a^b is %f  \n",pow(a,b));
    
    return 0;
}